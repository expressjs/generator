
let checkAppHealth = () => {
    //implement health checks like database connectivity or external dependencies configuration.
    return true;
}

module.exports = {
    checkAppHealth: checkAppHealth
}